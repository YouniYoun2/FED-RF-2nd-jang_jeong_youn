바렛콘스트 (수업에 늦게 들어와서 수업자료 보고 공부하는~.~)
JS2-2.바렛콘스트.html


var, let, const
var : 변수 (Variable)
let : 블록변수 (중괄호는 모두 블록임)
const : 상수 (constant) 변하지 않는 값

var는 위에서 호출하고 아래에서 선언 가능
let은 선언 후 아래에서 사용해야 함 <- ?
const 선언과 동시에 할당해야하고 한번 할당한 값은 변경 불가
var는 중복선언 가능
let은 같은 영역에서 같은 이름의 변수 중복선언 불가(전역과 지역을 별도로 관리해서 같은 이름도 두 곳에선 다르게 인식)

[var]
function callMe() {
    var usa = "미국";
    console.log("3.함수안 지역 usa:", usa);


함수 바깥에서 변수를 선언하면 전역변수


hoist : 끌어올리다라는 뜻. 선언하기 전 위에서 변수가 호출되는 현상을 변수 호이스팅이라고 한다.
반복문에서 i : 시작이라는 뜻 initiate


150번째줄까지 봤음






=======================================
dl dt랑 ul li 차이가 뭐임